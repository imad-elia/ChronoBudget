# Store assets

`metadata/en-US/` holds draft App Store / Play Store listing copy (short/full description, keywords, release notes) in the standard fastlane/EAS-submit layout.

## Still needed (manual, not automatable)
- Screenshots for each store's required device sizes (iPhone, iPad if supporting tablets, Android phone/tablet) — must be captured from a real build or simulator.
- App Store Connect / Play Console app icon exports if different from `assets/images/icon.png`.
- A real contact email in `docs/privacy-policy.html` (currently a `TODO` placeholder).
