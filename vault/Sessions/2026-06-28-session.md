# Session — 2026-06-28

Initial vault generated from existing project on 2026-06-28.

## What was done in this session (prior to vault creation)

- Implemented History screen (`app/(tabs)/history.tsx`) with date-grouped `SectionList` and category filter chips
- Implemented per-category budget limits with BentoCard progress bars and a `LimitsModal` bottom sheet
- Removed `react-native-unistyles` (JSI-only, breaks Expo Go on Android); replaced with static `theme/index.ts` + `StyleSheet.create`
- Fixed keyboard hiding input fields (`KeyboardAvoidingView`)
- Fixed Dashboard header hidden behind phone status bar (`useSafeAreaInsets` + `topInset` prop)
- Fixed Reanimated layout animation / transform conflict (split into two nested `Animated.View`)
- Added SQLite schema v3 migration: `subcategory` column on `transactions`, `app_settings` table
- Implemented dual input mode (Fast / Detailed) in `ExpenseInput.tsx`
- Added first-time onboarding overlay (`OnboardingOverlay.tsx`, 4 steps)
- Wired onboarding into Dashboard: shown once on first launch, persisted via `app_settings`

## Related notes

- [[ChronoBudget]]
- [[open-issues]]
