# Session — 2026-07-21

## Work done

### Feature — edit transaction (full edit)
- New `updateTransaction(id, {amount, category, subcategory, note})` in `db/database.ts` (modeled on `updateRecurring`).
- New `components/EditTransactionModal.tsx` — bottom-sheet form (category chips, amount, subcategory chips + custom, note, delete button), pattern copied from `RecurringModal`. Prefills from the transaction; a stored subcategory not in the presets is detected as custom and shown in the custom input.
- Wired to `onPress` of `TransactionRow` (dashboard `index.tsx`) and `HistoryRow` (`history.tsx`); both keep the X quick-delete. Save → `updateTransaction` → `triggerRefresh()`.
- i18n: `edit.*` keys in `en.ts`.

### Feature — starting balances (Remaining = balance − spent)
- **Schema v6**: `category_balance(category PK, amount REAL CHECK >= 0)`; helpers `fetchBalances()` / `setBalance(cat, amt)` (≤ 0 deletes the row, same convention as `setLimit`).
- Store: `balances` + `setBalances`, loaded in the dashboard fetch effect alongside limits.
- Onboarding: new skippable `balance` phase between country and tour (3 amount inputs, Continue / "Skip for now"). Uses the flex-free footer-pinned button (see 2026-07-03 note's Yoga lesson).
- `BentoCard`: new `balance` prop → "$X left" line under the amount; magenta when negative. Cards without a balance are unchanged.
- `SettingsModal`: "Starting balances" section (3 prefilled inputs; saved on Done or backdrop dismiss). Modal now wrapped in `KeyboardAvoidingView`.
- i18n: `onboarding.balance*`, `settings.balances`, `settings.balancesHint`, `card.remaining`.

### Persistence audit (user report: "data was gone on iOS")
- Audited onboarding gating in `index.tsx`: correct — `showOnboarding` starts false and only flips after `getSetting('onboarding_complete')` resolves falsy. No flash, no re-show path. **No code change**; the wipe was caused by our `simctl uninstall` debugging on 2026-07-03. Web in-memory reset remains a documented non-issue.

### Verified (web preview)
- Onboarding: country → balance (both value and skip paths) → tour.
- Balances persist and render ("$500.00 left" etc.); adding a $45 expense reduced Needs Remaining to $455 live.
- Edit modal opens prefilled from a row tap; amount edit ($10 → $25) saved and refreshed totals + row instantly; modal close state confirmed via React fiber (`visible=false`).
- Overspend: balance 50, spend 60 → magenta "-$10.00 left".
- `npx tsc --noEmit`: only the pre-existing unrelated errors.

### Tooling discovery (preview-only, NOT an app bug)
In the embedded preview browser, CSS `animationend` never fires, so react-native-web `Modal` fade-outs never complete — dismissed modals leave zombie DOM even though React state is correctly `visible=false`. When verifying modal dismissal there, inspect React fiber props or downstream state effects, not DOM presence. Real browsers and devices are unaffected (and native iOS uses native modals entirely).

### Follow-up — web onboarding/balances "reset" report (investigated, no bug)
User reported: onboarding restarts from the beginning on every browser refresh, and starting balances entered during onboarding "are not taken into consideration." Asked user to confirm platform: web browser.

Root cause confirmed by direct testing, not just re-reading the existing comment: `db/database.ts` uses `:memory:` SQLite on web, which resets on every page reload — both symptoms (onboarding flag gone, balances gone) are the same reset, not two separate bugs. Verified live:
- With a single tab, reload → onboarding correctly reappears (fresh empty DB, as designed).
- Within one session (no reload), setting balances (300/150/800) and continuing immediately rendered all three "$X left" lines correctly — the save/apply path itself has no bug.
- Along the way hit a real but unrelated error — `NoModificationAllowedError: createSyncAccessHandle ... Access Handle cannot be created if there is another open Access Handle` — caused by having two preview tabs open to the same origin at once (an OPFS-style lock race). This is a dev-tooling artifact from testing, not something end users hit; closing the stale tab resolved it.

User decision: leave web behavior as-is (documented, expected); do not build web persistence. No code changes made for this item.

### Follow-up — BentoCard "Remaining" line was too faint
User: the starting-balance "$X left" line is visible but "very faint and easily miss-able." Root cause: `styles.remaining` used `theme.typography.labelSmall` (10px) in `theme.colors.textMuted` (`#4A5168`) — nearly the same value as the card's near-black gradient background.

Fix in `components/BentoCard.tsx`:
- Size bumped from `labelSmall` (10px) to `bodyMedium` (14px), `fontWeight: '600'`.
- Color changed from `textMuted` (#4A5168) to `textSecondary` (#8B92A5) for the positive case; the existing negative-balance override to neon pink (`#FF2D78`) is unchanged.
- Verified via computed styles in the browser (not just visually): positive case renders `14px / 600 / rgb(139,146,165)`; negative case (added a $350 expense against a $300 Needs balance) renders `14px / 600 / rgb(255,45,120)`.
- `npx tsc --noEmit`: only the pre-existing unrelated errors.

## iOS test instructions (user)
`git pull` on the VM, then just reload Metro (`r`) — schema migration v6 runs automatically on next launch; no prebuild/rebuild, no uninstall (data persists).

## Related notes
- [[ChronoBudget]]
- [[open-issues]]
- [[2026-07-03-session]]
- [[category-balance-schema]]
