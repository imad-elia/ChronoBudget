# ChronoBudget

## What it does

Offline-first mobile expense tracker built for OLED dark-mode phones. Users log spending in one of three budget categories (Needs, Wants, Savings), set monthly limits per category, and review history. All data lives on-device in SQLite — no backend, no accounts.

## Stack

| Layer | Technology |
|-------|-----------|
| Framework | Expo SDK 56 / React Native 0.85.3 |
| Navigation | Expo Router (file-based tabs) |
| Database | expo-sqlite (WAL mode, schema versioning via `PRAGMA user_version`) |
| State | Zustand 5 |
| Animations | react-native-reanimated 4 |
| Styling | Plain `StyleSheet.create` + static `theme` object (no style libraries) |
| Icons | `@expo/vector-icons` (MaterialCommunityIcons) |
| Gradients | expo-linear-gradient |
| Safe area | react-native-safe-area-context |

## Current status (as of 2026-08-29)

- Dashboard screen: fully working
- History screen: fully working (+ CSV export on native)
- Trends screen: implemented (custom bar chart, last 6 months) — see [[Components]]
- Onboarding overlay: implemented (4 steps)
- Dual input mode (Fast / Detailed): implemented with subcategory chips
- Smart input: Fast free-text ("15 coffee" → Wants · Dining) + Detailed auto-suggest, with a local keyword classifier (~350-word seed dictionary, per-language-ready) that learns from corrections, plus a "My Keywords" screen (Settings) for directly adding/editing/deleting keyword → category/subcategory mappings. See [[smart-input-classifier]].
- Localization: country picker (onboarding + Settings) sets currency + number formatting app-wide; English-only display with i18n-ready string files. See [[localization]].
- Budget limits: implemented with progress bars in BentoCards; over-limit state shows the true % (uncapped) + a magenta OVER badge.
- Recurring transactions: weekly/monthly/yearly rules (Recurring manager modal, autorenew header icon) that auto-post real transactions on app open, with month-end-clamped date math and missed-period catch-up. Optional custom start date at creation (hand-rolled month-grid picker, `components/DatePickerField.tsx`) — past dates immediately catch up, future dates wait. See [[recurring-transactions]].
- Web build: functional. Web uses in-memory SQLite (dev preview); native uses persistent WAL. See [[web-inmemory-db]].
- Edit transaction: tap any row (dashboard or history) to open a full edit modal (amount/category/subcategory/note + delete). See [[2026-07-21-session]].
- Starting balances: optional per-category one-time balances (onboarding step + Settings); BentoCards show "left" (Remaining = balance − spent, magenta when negative). Schema v6 `category_balance` — see [[category-balance-schema]].
- iOS build: verified working on iOS Simulator (iPhone 17 Pro, iOS 26.5) via macOS 13 VM (Intel, Xcode 26.5). `ios.bundleIdentifier` set in `app.json`. Onboarding country picker redesigned as a bounded table panel; native-only flex-collapse bug fixed. See [[2026-07-03-session]].
- Automated tests: `jest-expo` + `@testing-library/react-native`, run via `npm test` (153 real Jest tests as of 2026-07-27, later session — up from 246 minus a recount after adding a new `GoalsModal.test.tsx` suite; see note below on the stale worktree inflating earlier counts). Covers `lib/detectCategory.ts` (incl. French dictionary + accent-stripping), `lib/recurrence.ts`, `lib/format.ts`, `lib/csv.ts`, `store/useBudgetStore.ts` (incl. language/account/goal state), `components/BentoCard.tsx`/`ExpenseInput.tsx`/`EditTransactionModal.tsx`/`RecurringModal.tsx`/`KeywordsModal.tsx`/`DatePickerField.tsx`/`GoalsModal.tsx`, and `db/database.ts` (schema migrations + idempotency, CRUD, `processRecurring`, accounts/transfers, goals, bulk import) against a `sql.js`-backed mock of `expo-sqlite`. Plus 3 Playwright E2E specs (`e2e/`) driving the Expo web build end-to-end (onboarding, add/edit/delete a transaction, History/Trends navigation). CI (`.github/workflows/ci.yml`) runs the unit suite + `tsc --noEmit` + the E2E suite (parallel jobs) on every push/PR to `main`. See [[testing-strategy]]. **Superseded by the 2026-08-29 automation pass — see the entry below for current counts and coverage.** The stray `.claude/worktrees/exciting-bose-b67c07/` copy is no longer test-affecting: it was silently winning mock resolution, and Jest now excludes it via `modulePathIgnorePatterns`, so plain `npm test` is trustworthy again. The directory itself still exists and is the user’s to remove.
- Dashboard totals are month-scoped: `fetchCategoryTotals()` in `db/database.ts` now defaults to the current month (`currentMonthKey()`, using `strftime(..., 'localtime')` to stay consistent with JS's local-time month key), with `fetchCategoryTotals(null)` for all-time. Header label changed from "Total Spent" to "Spent This Month". Fixed 2026-07-27.
- Full UI translation: French now ships (`constants/i18n/fr.ts`, ~100 keys); language now has its own independent picker (see below) rather than being tied only to country selection. Fixed several frozen-label bugs and a `KeywordsModal` reactivity gap along the way, and routed the last ~15 hardcoded UI strings (category names, empty states, budget-limits modal, onboarding tour buttons) through `t()`. See [[localization]].
- Smart input: fuzzy/stemming matching implemented — hand-rolled two-pass fallback (stemming + bounded Levenshtein) in `lib/detectCategory.ts`, no new dependency. Exact matches always still win over fuzzy ones. See [[smart-input-classifier]].
- `LimitsModal`'s hardcoded `$` currency prefix fixed — now subscribes to `symbol` from `useBudgetStore` like every other money-input surface. Fixed 2026-07-27.
- Non-English smart-input keyword dictionary: French seed dictionary (`constants/keywords/fr.ts`, ~250 entries) added alongside the English one, with per-language stemming (`lib/detectCategory.ts`) and accent-insensitive matching (NFD diacritic stripping) so "café"/"cafe" both resolve. `constants/keywordMap.ts` is now a live accessor (`getActiveKeywordMap()`) instead of a frozen constant, reacting to language changes at runtime.
- Independent language selector: `language` is now a store field separate from `country`/`locale` (`store/useBudgetStore.ts`), persisted via `app_settings`. Country selection still sets a sensible default language on first pick, but never overrides an explicit choice made via the new picker in `SettingsModal.tsx` (English/Français for now — the two locales with real translation + keyword bundles).
- Account-aware budgeting: schema v7 adds `accounts` and `transfers` tables plus a nullable `account_id` on `transactions`/`recurring`. Transactions can optionally tag an account (chips in `ExpenseInput.tsx`/`EditTransactionModal.tsx`); account balances update atomically on insert/edit/delete. Transfers move money between two accounts without touching budget-category totals (they live outside the `transactions` table). Managed via the new `AccountsModal.tsx`, reachable from Settings. See [[account-aware-budgeting]].
- CSV round-trip import: `lib/csv.ts` parses CSVs matching the app's own export format only (no generic bank-CSV mapping); malformed rows are skipped and counted rather than aborting the import. Import button added next to Export in History (web: `<input type="file">`; native: new `expo-document-picker` dependency + `expo-file-system`). Bulk-inserted via `insertTransactionsBulk()`.
- Recurring rules can now optionally tag an account too, same chip picker as one-off transactions (`RecurringModal.tsx`); `processRecurring()`'s posting loop applies the matching account-balance debit per posted occurrence. Dashboard also gained a small read-only accounts-balance row (`DashboardHeader` in `index.tsx`, tap to open `AccountsModal`), hidden when no accounts exist. See [[account-aware-budgeting]].
- Savings goals (sinking funds): schema v8 adds a `goals` table (name, target amount, running current amount) plus a nullable `goal_id` on `transactions`. Users can create named goals (e.g. "Car repair fund") via the new `GoalsModal.tsx` (reachable from Settings), tag Savings-category transactions to a goal via a chip picker in `ExpenseInput.tsx`/`EditTransactionModal.tsx` (hidden unless category is Savings and at least one goal exists), and see progress via a shared `components/ProgressBar.tsx` (extracted from `BentoCard.tsx`). A goal's `current_amount` only grows through tagged transactions — no direct balance edit. Dashboard gained a matching read-only goals-progress row next to the accounts row. See [[savings-goals-schema]].
- App/Play Store submission readiness: an audit confirmed CI only ever exercises the web build (Jest + Playwright), never a native iOS/Android build — expected, since automated tests validate logic, not store compliance. Filled in the config/metadata gaps that actually blocked a native build or submission: `app.json` gained `android.package`/`android.versionCode`, `ios.buildNumber`, and an `ios.privacyManifests` block (declares `FileTimestamp`/`DiskSpace` "required reason" API categories tied to `expo-file-system`'s CSV export/import usage). New `eas.json` (development/preview/production build profiles + a submit skeleton, no fabricated credentials). New `docs/privacy-policy.html` (static page, meant for GitHub Pages) and `store-assets/metadata/en-US/*.txt` draft listing copy. See [[store-submission-readiness]] — screenshots and a real contact email are explicitly still needed (manual, not automatable) before actual submission.
- Subcategory names (Rent, Dining, Emergency Fund, etc.) are now translated for display in French — previously they stayed in English everywhere, including in Edit Transaction (user-reported). New `subcategoryLabel()` helper in `constants/subcategories.ts` translates the canonical stored string for display only; DB storage and keyword-dictionary matching are unchanged. Applied across `ExpenseInput.tsx`, `EditTransactionModal.tsx`, `KeywordsModal.tsx`, `RecurringModal.tsx`, and the Dashboard/History recent-transaction rows. See [[localization]], [[open-issues]]. Fixed 2026-07-27. Also found (not fixed): the onboarding tour's step copy (`OnboardingOverlay.tsx`) is still hardcoded English — see [[open-issues]].
- Also fixed (2026-07-27, later): `EditTransactionModal.tsx`/`RecurringModal.tsx` category labels were frozen to first-load language (same bug class, different files); the tab bar didn't update on a live language switch (wrong forcing-subscription field — fixed to subscribe to `language` directly); Settings' country list is now a collapsed dropdown instead of an always-expanded 20-row list; Settings header icon reordered so gear/Settings is rightmost. All confirmed working by the user 2026-08-29. See [[localization]], [[open-issues]].
- Onboarding tour copy (Welcome/Fast Mode/Detailed Mode/Budget Limits) is now translated via `t()` — the last untranslated onboarding surface. Also confirmed (no new code needed): first-time language selection already gets inferred from device location, since the country picker pre-fills from `expo-localization` and `setCountry()` already defaults `language` from the picked country on a genuine first pick — the tour now inherits that automatically since it renders after the country step. Fixed 2026-08-29, awaiting user manual verification. See [[localization]].
- Onboarding now supports backward navigation across all three phases (`tour` → `balance` → `country`), so a wrong country/currency pick is always fixable without restarting — the balance step gained a "‹ Back" link, and the tour's first step's previously-hidden Back button now returns to the balance phase. The tour's Back button also moved to sit alongside "Skip tutorial" as a matching text link instead of a bordered button. Fixed 2026-08-29, awaiting user manual verification. See [[localization]].
- Test automation pass 2026-08-29 (Tiers 1+2 of the manual plan): 188 unit tests (was 157) across 16 suites, 16 Playwright specs (was 3), a three-timezone CI matrix, and four static checks run as a new `checks` CI job (`npm run check`) — frozen `t()` at module scope, hardcoded UI strings, release config plus the offline claim, and WCAG contrast. Found and fixed two test-infrastructure defects (the sqlite mock never opened a real transaction; Jest was resolving the stale worktree copy of that mock, so the DB suite had never run the project’s own) and a seventh live frozen-label bug in `RecurringModal`. Native coverage is deliberately still zero — see [[testing-strategy]] “What is deliberately not covered”. See [[2026-08-29c-session]], [[open-issues]].
- Production-readiness QA test plan authored 2026-08-29 (190 manual cases, 15 functional + 11 technical suites) — published as an artifact, none of it executed yet. Writing it surfaced three defects by code review, all fixed the same day with regression tests: `fetchMonthlyTotals()` bucketed months in UTC while `fetchCategoryTotals()` used local time (Trends disagreed with the Dashboard near month boundaries outside UTC); `deleteAccount()`'s reference guard didn't count `transfers`, so an account referenced only by a transfer bypassed it into a foreign-key exception; and `goals.target_amount` had no positive constraint. See [[2026-08-29b-session]], [[open-issues]].
- Fast-mode smart input now saves its leftover typed text as the transaction's note (e.g. "10 coffee" → Wants · Dining with note "coffee") instead of silently discarding it — was a one-line bug (Fast-mode submissions unconditionally passed an empty note). Fixed 2026-08-29, awaiting user manual verification. See [[smart-input-classifier]].

## Known issues

See [[open-issues]].

## Next steps

- Non-English keyword dictionaries beyond French (e.g. German, Spanish) for locales that already have translation bundles pending, or new locales entirely. Re-verified 2026-07-27 (later session): still the only genuinely open "next step" — English/French remain the only two languages with both an i18n bundle and a keyword dictionary.
- CSV import stays intentionally round-trip-only (this app's own export format); generic bank-CSV import with column mapping would be a separate, larger feature — confirmed out of scope for now.
- Android payment-notification auto-entry was evaluated (2026-07-21) and shelved: requires a native notification-listener service (no Expo Go support, needs EAS dev build), manual per-user permission grant, Play Store declared-use justification, and fragile per-bank text parsing. Not started.
- Savings goals (just added) don't yet support recurring-rule tagging (`RecurringModal.tsx`) — a recurring Savings contribution can't auto-post toward a specific goal. Judged a plausible future extension, not required for v1 — see [[savings-goals-schema]].
- Store submission: screenshots (real device/simulator captures) and a real contact email in `docs/privacy-policy.html` are still needed before actually submitting. Running `eas build`/`eas submit` requires the user's own Apple Developer/Google Play accounts and credentials — not something that can be done from this environment. See [[store-submission-readiness]].

## Related notes

- [[Overview]] — architecture
- [[Components]] — file-by-file breakdown
- [[DataFlow]] — how data moves
- [[testing-strategy]] — automated test suite setup
- [[account-aware-budgeting]] — accounts + transfers schema decision
- [[savings-goals-schema]] — savings goals (sinking funds) schema decision
- [[store-submission-readiness]] — app.json/eas.json config, privacy manifest, store metadata
- [[localization]] — independent language selector, French keyword dictionary
